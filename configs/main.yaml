defaults:
  - _self_
  - dataset: cyclone
  - model: swin5d
  - logging: wandb
  - training: default
  - validation: default
  # - override hydra/launcher: litai

experiment_id: test
seed: 0
output_path: null
use_ddp: false
use_amp: true  # set to false when using integrals in training (complex / fft)
mode: "train"
eval: true
device: "cuda"
load_ckp: false