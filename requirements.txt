# Core JAX stack
jax>=0.4.30
jaxlib>=0.4.30
equinox>=0.11.0
optax>=0.2.0
ml-dtypes>=0.4               # bf16 numpy dtype (quantize pipeline + bf16 reads)

# Hydra / config / yaml
hydra-core>=1.3
omegaconf>=2.3
pyyaml>=6.0

# Numerics & I/O
numpy>=1.26
scipy>=1.11                  # linear_sum_assignment for minibatch-OT in flow matching
einops>=0.7
tqdm>=4.65

# Logging
wandb>=0.16                  # optional at runtime; logger no-ops if missing

# Plotting (eval / parity figures)
matplotlib>=3.8

# Checkpointing — eqx tree pickling is built into equinox; orbax is optional but recommended for multi-host
orbax-checkpoint>=0.5.0

# ---- Optional extras (install separately depending on the box) -------
# GPU-direct dataloading (KvikIO + cupy → JAX via dlpack):
#   pip install cupy-cuda12x kvikio-cu12
#
# Physics integrals (gyrokinetic flux + phi solver in pure JAX):
#   pip install "gyaradax @ git+https://github.com/gerkone/gyaradax.git@master"
#
# Parity tests against the torch reference repo (one level up):
#   pip install torch h5py
